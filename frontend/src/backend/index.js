import * as userService from './userService';
import * as pacienteService from './pacienteService';
import NetworkError from './NetworkError';

export { NetworkError };
export default { userService, NetworkError, pacienteService };
